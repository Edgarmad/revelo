---
import { navItems } from '@/data/siteContent';
import Container from '@/components/common/Container.astro';
import { ChevronDown, Menu, X } from 'lucide-astro';
import { getAllCategories } from '@/services/categoryService';
import { getFeaturedProducts } from '@/services/productService';
import logoSrc from '@/styles/images/Logo Mila.svg';

const menuCategories = getAllCategories();
const menuProducts = getFeaturedProducts(4);
---

<header class="sticky top-0 z-50 bg-white shadow-sm">
  <div class="bg-ink py-2 text-white">
    <Container class="flex items-center justify-center gap-3 text-xs font-bold">
      <span>Oferta de verano - Hasta 50% de descuento</span>
      <a class="rounded-pill bg-aqua px-4 py-1 text-ink" href="/products/">Ver ofertas</a>
    </Container>
  </div>
  <Container class="flex min-h-[82px] items-center justify-between gap-4 lg:grid lg:grid-cols-[188px_minmax(0,1fr)_128px]">
    <a href="/" class="inline-flex items-center" aria-label="Milapro Home">
      <img class="h-auto w-[188px] max-w-[42vw]" src={logoSrc} alt="Milapro Home" width="188" height="25" />
    </a>
    <nav class="hidden items-center gap-[34px] text-sm font-bold text-[#332b1e] lg:flex lg:justify-self-center" aria-label="Navegacion principal">
      {
        navItems.map((item) => {
          if (item.label === 'Muebles De Exterior') {
            return (
              <div class="group py-8">
                <a class="inline-flex items-center gap-1 transition group-hover:text-aqua focus-visible:text-aqua" href={item.href}>
                  {item.label}
                  <ChevronDown class="transition group-hover:rotate-180" size={14} />
                </a>
                <div class="pointer-events-none absolute left-0 top-full w-full translate-y-2 border-t border-ink/10 bg-white opacity-0 shadow-2xl transition duration-200 group-hover:pointer-events-auto group-hover:translate-y-0 group-hover:opacity-100 group-focus-within:pointer-events-auto group-focus-within:translate-y-0 group-focus-within:opacity-100">
                  <Container class="grid gap-12 py-12 lg:grid-cols-[0.95fr_1.05fr]">
                    <div>
                      <p class="text-sm font-black uppercase tracking-[0.18em] text-aqua">Muebles de exterior</p>
                      <h2 class="mt-4 font-display text-5xl font-light uppercase tracking-wide text-ink">Compra por categoria</h2>
                      <div class="mt-8 flex flex-wrap gap-3">
                        {menuCategories.map((category, index) => (
                          <a class={`rounded-pill border px-5 py-2 text-xs font-semibold transition ${index === 0 ? 'border-aqua bg-aqua text-ink' : 'border-ink/25 hover:border-aqua hover:text-aqua'}`} href={`/categories/${category.slug}/`}>
                            {category.name}
                          </a>
                        ))}
                      </div>
                    </div>
                    <div class="grid gap-4 sm:grid-cols-2">
                      {menuCategories.slice(0, 4).map((category) => (
                        <a class="group/card" href={`/categories/${category.slug}/`}>
                          <img class="aspect-[1.35/1] w-full rounded-xl bg-sand object-cover transition group-hover/card:scale-[1.02]" src={category.image} alt={category.name} loading="lazy" width="500" height="370" />
                          <p class="mt-2 text-center text-xs font-semibold text-muted">{category.name}</p>
                        </a>
                      ))}
                    </div>
                  </Container>
                </div>
              </div>
            );
          }

          if (item.label === '🔥 Productos') {
            return (
              <div class="group py-8">
                <a class="inline-flex items-center gap-1 transition group-hover:text-aqua focus-visible:text-aqua" href={item.href}>
                  {item.label}
                  <ChevronDown class="transition group-hover:rotate-180" size={14} />
                </a>
                <div class="pointer-events-none absolute left-0 top-full w-full translate-y-2 border-t border-ink/10 bg-white opacity-0 shadow-2xl transition duration-200 group-hover:pointer-events-auto group-hover:translate-y-0 group-hover:opacity-100 group-focus-within:pointer-events-auto group-focus-within:translate-y-0 group-focus-within:opacity-100">
                  <Container class="grid gap-12 py-12 lg:grid-cols-[0.95fr_1.05fr]">
                    <div>
                      <p class="text-sm font-black uppercase tracking-[0.18em] text-aqua">Productos</p>
                      <h2 class="mt-4 font-display text-5xl font-light uppercase tracking-wide text-ink">Colecciones destacadas</h2>
                      <div class="mt-8 grid max-w-xl gap-6">
                        {menuCategories.slice(0, 3).map((category) => (
                          <a class="flex items-center justify-between border-b border-ink/10 pb-5 text-3xl font-light uppercase tracking-wide text-ink transition hover:text-aqua" href={`/categories/${category.slug}/`}>
                            {category.name}
                            <span class="grid h-9 w-9 place-items-center rounded-full border border-ink/30 text-xl leading-none">↗</span>
                          </a>
                        ))}
                      </div>
                    </div>
                    <div class="grid gap-4 sm:grid-cols-2">
                      {menuProducts.map((product) => (
                        <a class="group/card" href={`/products/${product.slug}/`}>
                          <img class="aspect-[1.35/1] w-full rounded-xl bg-sand object-cover transition group-hover/card:scale-[1.02]" src={product.mainImage} alt={product.name} loading="lazy" width="500" height="370" />
                          <p class="mt-2 text-center text-xs font-semibold text-muted">{product.name}</p>
                        </a>
                      ))}
                    </div>
                  </Container>
                </div>
              </div>
            );
          }

          return (
            <a class="transition hover:text-cocoa" href={item.href}>
              {item.label}
            </a>
          );
        })
      }
    </nav>
    <a class="hidden h-11 items-center justify-center rounded-pill bg-aqua px-6 text-sm font-black uppercase text-ink transition hover:bg-ink hover:text-white lg:inline-flex" href="/contact/">Contactar</a>
    <button class="grid h-11 w-11 place-items-center rounded-full border border-ink/15 lg:hidden" type="button" data-menu-toggle aria-expanded="false" aria-controls="mobile-menu" aria-label="Abrir menu"><Menu size={22} /></button>
  </Container>
  <div id="mobile-menu" class="hidden border-t border-ink/10 bg-white lg:hidden" data-mobile-menu>
    <Container class="py-5">
      <div class="mb-4 flex items-center justify-between">
        <span class="font-bold">Menu</span>
        <button class="grid h-10 w-10 place-items-center rounded-full border border-ink/15" type="button" data-menu-close aria-label="Cerrar menu"><X size={20} /></button>
      </div>
      <nav class="grid gap-3 text-base font-bold" aria-label="Navegacion movil">
        {navItems.map((item) => <a class="rounded-xl bg-sand px-4 py-3" href={item.href}>{item.label}</a>)}
        <a class="rounded-xl bg-aqua px-4 py-3 text-center font-black uppercase text-ink" href="/contact/">Contactar</a>
      </nav>
    </Container>
  </div>
</header>

<script>
  const toggle = document.querySelector<HTMLButtonElement>('[data-menu-toggle]');
  const close = document.querySelector<HTMLButtonElement>('[data-menu-close]');
  const menu = document.querySelector<HTMLElement>('[data-mobile-menu]');
  const setOpen = (open: boolean) => {
    menu?.classList.toggle('hidden', !open);
    toggle?.setAttribute('aria-expanded', String(open));
  };
  toggle?.addEventListener('click', () => setOpen(menu?.classList.contains('hidden') ?? true));
  close?.addEventListener('click', () => setOpen(false));
</script>
